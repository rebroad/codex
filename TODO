Urgent: Move .codex/rules into repos (otherwise wastes input tokens as all rules sent with every prompt!)
Show new memories (Optional. When memory files are updated, show this to the user)
Fix ~/.codex/memories read-only issue when the project is $HOME !
A prompt of "." can be ignored but can unpause a session.
Find the difference in "executed commands", see https://github.com/openai/codex/issues/37815
Extend `/ps` - and never send / commands to the model!
poll counting does not include every backend request - fix
codeburn watch reports the wrong model (when a model is changed mid-session)
Make Ctrl+C exit the TUI, NOT abort the job (let Esc do that)
Fix caching issue with codex-auto-review (too many cache misses!)
Add a progress report/bar for `codex resume` given how slow it can be!
Add ability to clear/delete a queued steer in the TUI (delete key?)
Fix `just fmt` failing when the sandbox cannot download the pinned `ruff`/`maturin` packages from PyPI.
Make app-server daemon restart reliably replace the remote-control server and wait for the control socket so clients can reconnect.
Verify session and turn lifecycle state across app-server replacement, including stale `Working` UI state and `turn/interrupt` returning `no active turn`.
